{
  "name": "owasp-shield-scanner-backend",
  "version": "1.0.0",
  "description": "Real-time security scanner backend for OWASP Shield Desk. Runs genuine checks (port scan, headers, TLS, exposed files, tech detection, subdomains) and optional CLI tools (nmap/nuclei).",
  "type": "module",
  "main": "src/index.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.55.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "ws": "^8.18.0"
  }
}
